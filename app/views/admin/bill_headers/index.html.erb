<h1>Listing bill_headers</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bill_headers.each do |bill_header| %>
  <tr>
    <td><%= link_to 'Show', bill_header %></td>
    <td><%= link_to 'Edit', edit_bill_header_path(bill_header) %></td>
    <td><%= link_to 'Destroy', bill_header, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill header', new_bill_header_path %>
