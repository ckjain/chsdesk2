<%= form_for(@bill_header) do |f| %>
  <% if @bill_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_header.errors.count, "error") %> prohibited this bill_header from being saved:</h2>

      <ul>
      <% @bill_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
